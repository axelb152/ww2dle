# Supabase project settings → API. The anon key is public (safe in a static
# build); real values go in GitHub repo secrets for the deployed site.
REACT_APP_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key
